# Honest per-ADR conformance ledger (ADR-ERP-020, Phase 30). This is checked in by hand
# and reviewed alongside every architecturally significant change; it is not generated
# from a coverage tool, so keep entries accurate rather than optimistic.
#
# status:
#   foundation - the decision has real, working code demonstrating the pattern
#   partial    - some of the decision is implemented and tested; a material gap remains
#   planned    - decided in the ADR, not yet implemented
schema_version: "1.0"
updated: "2026-09-07"
adrs:
  - id: ADR-ERP-001
    title: iDempiere as an isolated, headless, multi-tenant Baobab ERP engine
    status: foundation
    evidence:
      - idempiere/Dockerfile
      - compose.yaml
      - idempiere/extensions/
    gap: No production EngineInstance exists; no application-service HTTP layer yet.

  - id: ADR-ERP-002
    title: ERP tenant, client and organization mapping
    status: partial
    evidence:
      - db/migrations/0002_create_tenant_mapping.sql
      - db/migrations/0006_add_tenant_mapping_native_unique_index.sql
      - modules/context/postgres_store.py
      - modules/application/server.py
      - tests/integration/test_postgres_context_store.py
      - tests/integration/test_http_server.py
      - tests/tenancy/test_context_resolver.py
      - idempiere/extensions/org.nabhold.baobab.erp.context/
      - idempiere/extensions/org.nabhold.baobab.erp.integration/
      - idempiere/extensions/org.nabhold.baobab.erp.events/
    gap: >
      modules/context resolves against a real Postgres-backed store, exposed over
      HTTP at GET /context/resolve. BaobabContextResolver (the OSGi bundle running
      inside iDempiere) now calls that endpoint for real -- tested against a real
      local HTTP server reproducing the exact response shape -- instead of always
      failing closed. The forward direction (tenant -> AD_Client/AD_Org) still has
      no invoking extension point inside iDempiere: it's driven by inbound requests,
      which still depends on the deferred REST API plugin (ADR-ERP-005/ADR-ERP-013).
      The reverse direction (AD_Client/AD_Org -> tenant), added alongside this note
      as ContextResolver.resolveTenant / GET /context/resolve-tenant, backed by a new
      unique index on baobab.tenant_mapping(ad_client_id, ad_org_id) WHERE
      status='active' so it's unambiguous, DOES now have a real invoking extension
      point: the `events` bundle calls it on every real C_BPartner/M_Product/C_Order/
      C_Invoice create/update to derive the correct tenant per record (ADR-ERP-003's default
      topology puts several AD_Clients/tenants on one iDempiere runtime, so a single
      process-wide tenant would be wrong) -- see ADR-ERP-007's evidence below.

  - id: ADR-ERP-003
    title: ERP EngineInstance isolation, regional deployment and data residency
    status: planned
    evidence: []
    gap: No EngineInstance/IsolationProfile implementation exists in this repository yet; owned jointly with nabhold/baobab-cp.

  - id: ADR-ERP-004
    title: Baobab iDempiere extension architecture
    status: foundation
    evidence:
      - idempiere/extensions/org.nabhold.baobab.erp.context/
      - idempiere/extensions/org.nabhold.baobab.erp.mapping/
      - idempiere/extensions/org.nabhold.baobab.erp.integration/
      - idempiere/extensions/org.nabhold.baobab.erp.events/
      - idempiere/patches/README.md
    gap: >
      4 of the 8 reserved namespaces have a bundle; the rest are unimplemented by
      design until there is a real extension point to fill. `integration` is a
      library bundle (no Activator, no published OSGi service) that `context` and
      `mapping` both depend on for reaching baobab-app over HTTP -- a real,
      inter-bundle OSGi dependency (Import/Export-Package), not just shared source.
      `events` goes further: it's the first bundle that consumes other bundles'
      OSGi *services* (context's ContextResolver and mapping's
      CanonicalMappingResolver, each via a ServiceTracker) rather than just sharing
      a library, and it registers a plain org.osgi.service.event.EventHandler for
      iDempiere's own asynchronous adempiere/po/postCreate and postUpdate topics --
      a real, working example of the "event handler" extension mechanism named
      throughout this ADR, requiring no iDempiere Maven artifact at compile time
      (see idempiere/README.md).

  - id: ADR-ERP-005
    title: Baobab ERP integration API architecture
    status: partial
    evidence:
      - modules/integration/idempiere_client.py
      - modules/integration/delivery_transport.py
      - modules/application/server.py
      - tests/unit/test_idempiere_client.py
      - tests/integration/test_http_server.py
    gap: >
      The inbound HTTP server exists and is tested end-to-end (health checks, signed
      webhook receipt). RestIdempiereClient is now a real implementation (one-step JWT
      login, automatic token refresh, get/create/update against /models/{table}),
      verified against the com.trekglobal.idempiere.rest.api / bxservice/idempiere-rest
      OpenAPI spec and tested against a fake server reproducing that spec exactly --
      but not yet against a real running iDempiere instance, because that REST API
      plugin is not part of the pinned idempiereofficial/idempiere image and is not
      yet installed by idempiere/Dockerfile (see idempiere/README.md). Until that
      installation is wired up, no canonical operation can actually reach a live
      iDempiere; that remains the most consequential gap in this ADR.

  - id: ADR-ERP-006
    title: ERP canonical event architecture
    status: foundation
    evidence:
      - modules/events/envelope.py
      - modules/outbox/postgres_store.py
      - modules/inbox/postgres_store.py
      - modules/application/server.py
      - modules/application/dispatch_worker.py
      - modules/scripts/dispatch_worker_loop.sh
      - modules/integration/delivery_transport.py
      - db/migrations/0004_create_event_outbox.sql
      - db/migrations/0005_create_event_inbox.sql
      - contracts/events/
      - compose.yaml
      - tests/integration/test_postgres_outbox_store.py
      - tests/integration/test_postgres_inbox_store.py
      - tests/integration/test_http_server.py
    gap: >
      Record -> dispatch -> signed HTTP delivery -> mark-delivered, and
      receive -> verify -> deduplicate -> persist, are both proven end-to-end
      against a real Postgres database and real HTTP server (not just in-memory
      fakes). dispatch_worker.py still runs to completion and exits by design
      rather than being a long-running daemon, but that gap is now closed: the
      baobab-dispatch-worker Compose service invokes it periodically via
      modules/scripts/dispatch_worker_loop.sh (a plain, configurable sleep loop --
      scheduling policy as deployment configuration, matching the worker's own
      design intent, not a change to the worker itself). What's still open: the
      loop is a shell sleep loop, not a real scheduler daemon (cron, a systemd
      timer) -- fine for the single-host Compose topology this repository
      supports today, but worth revisiting if/when ADR-ERP-013 moves past it; and
      there's no retry/backoff between ticks beyond dispatch_worker.py's own
      per-event backoff, so a failed tick just waits for the next fixed-interval
      attempt.

  - id: ADR-ERP-007
    title: ERP canonical entity and external reference mapping architecture
    status: partial
    evidence:
      - db/migrations/0003_create_entity_mapping.sql
      - modules/mapping/postgres_store.py
      - tests/integration/test_postgres_mapping_store.py
      - tests/integration/test_http_server.py
      - idempiere/extensions/org.nabhold.baobab.erp.mapping/
      - idempiere/extensions/org.nabhold.baobab.erp.integration/
      - idempiere/extensions/org.nabhold.baobab.erp.events/
    gap: >
      modules/mapping resolves canonical <-> native references against a real
      Postgres-backed store, exposed over HTTP at GET /mapping/resolve and
      /mapping/resolve-canonical. BaobabMappingResolver (the OSGi bundle) now calls
      those for real, tested against a real local HTTP server. The extension-point
      gap that used to sit here is now closed for section 170's near-term slice
      (Party/Product/PurchaseOrder-SalesOrder/Supplier-CustomerInvoice, per that
      section's own Definition-of-Done checklist): the `events` bundle registers a
      real org.osgi.service.event.EventHandler for iDempiere's own asynchronous
      adempiere/po/postCreate/postUpdate topics, filtered to C_BPartner, M_Product,
      C_Order and C_Invoice. On every such event it first derives the owning tenant
      from the changed record's own AD_Client_ID/AD_Org_ID via
      ContextResolver.resolveTenant (see ADR-ERP-002's evidence), then calls
      CanonicalMappingResolver.resolveToCanonical for that tenant -- a real
      iDempiere request now invokes both registered services, correctly, even when
      one iDempiere instance hosts several tenants at once (ADR-ERP-003's default
      topology), which an earlier version of this bundle got wrong (a single
      process-wide tenant system property, caught in review before merge). What's
      still open: C_Payment, M_InOut and M_Warehouse are in section 170's full
      mapping matrix but not yet in its checklist or wired here -- adding one is a
      one-line filter change in EventsActivator, not new code, since the handler
      reads "tableName" generically.

  - id: ADR-ERP-008
    title: ERP financial, accounting and multi-currency architecture
    status: planned
    evidence: []
    gap: No LegalEntity accounting configuration (chart of accounts, fiscal calendar, tax setup) exists yet.

  - id: ADR-ERP-009
    title: ERP market localisation, jurisdiction and regulatory architecture
    status: planned
    evidence: []
    gap: Not started.

  - id: ADR-ERP-010
    title: ERP security, identity, authorization and tenant-isolation architecture
    status: partial
    evidence:
      - modules/security/signing.py
      - modules/context/resolver.py
      - modules/context/postgres_store.py
      - modules/identity/service.py
      - modules/application/server.py
      - tests/tenancy/test_context_resolver.py
      - tests/integration/test_http_server.py
    gap: >
      Fail-closed context resolution and HMAC signature verification are now proven
      against a real database and a real HTTP round trip (valid signature accepted,
      invalid signature rejected with 401, before the envelope is even parsed), not
      just in-memory fakes. There is still no real IdP/authentication integration;
      ServiceIdentity remains a data carrier only, and the HTTP layer has no
      authentication of its own beyond the event-signing secret.

  - id: ADR-ERP-011
    title: ERP observability, audit, reconciliation and operational control architecture
    status: partial
    evidence:
      - modules/reconciliation/record_counts.py
      - modules/reconciliation/identity.py
      - modules/mapping/postgres_store.py
      - tests/unit/test_record_counts.py
      - tests/unit/test_identity_reconciliation.py
      - tests/integration/test_postgres_mapping_store.py
      - docs/reconciliation.md
    gap: >
      Two of section 63's reconciliation dimensions exist: count (section 68,
      pre-existing) and identity (section 64, new) -- do the canonical entities
      Baobab expects to have an active iDempiere mapping actually have one, and
      are there active mappings for entities no longer expected. Both are pure
      functions over injected stores, tested with in-memory fakes and against
      real Postgres rows (PostgresCanonicalMappingStore.active_canonical_ids).
      Neither is wired to a scheduler or exposed over HTTP -- there is no
      periodic job that calls them and no endpoint to trigger or report a run,
      unlike dispatch_worker.py's Compose-scheduled loop. State (section 65),
      quantity (section 66) and amount (section 67) reconciliation are not
      started, and audit/observability tooling generally (tracing, the
      erp_reconciliation_* metrics section 383/385 name) has not begun.

  - id: ADR-ERP-012
    title: ERP high availability, backup, disaster recovery and business continuity architecture
    status: planned
    evidence: []
    gap: Not started.

  - id: ADR-ERP-013
    title: ERP deployment, release, upgrade and configuration management architecture
    status: partial
    evidence:
      - upstream.lock.yaml
      - compose.yaml
      - idempiere/Dockerfile
      - modules/Dockerfile
      - .trivyignore
    gap: >
      compose.yaml now builds and runs a baobab-app image (modules/) alongside
      iDempiere, with a one-shot baobab-db-migrate service applying db/migrations
      before it starts. modules/Dockerfile is not yet covered by the
      foundation/vulnerability-scan workflow (that workflow currently scans only
      idempiere/Dockerfile). No CI image publishing, upgrade rehearsal, or
      release-manifest tooling yet. .trivyignore documents a scoped, dated risk
      acceptance for HIGH/CRITICAL CVEs in third-party libraries vendored inside the
      pinned idempiereofficial/idempiere 13-release image (no fixed build exists
      upstream as of 2026-09-06); it must be re-reviewed whenever
      upstream.lock.yaml's iDempiere pin changes. Not yet wired: installing the
      com.trekglobal.idempiere.rest.api plugin (a Maven/Tycho build + p2 provisioning
      step, not a jar copy) that RestIdempiereClient needs a live instance to expose
      -- see idempiere/README.md and ADR-ERP-005.

  - id: ADR-ERP-014
    title: ERP master data ownership, synchronisation and reference data architecture
    status: planned
    evidence: []
    gap: Not started.

  - id: ADR-ERP-015
    title: ERP inventory, warehouse, procurement and supply-chain architecture
    status: planned
    evidence: []
    gap: Not started.

  - id: ADR-ERP-016
    title: Commerce-ERP integration and order-to-cash architecture
    status: planned
    evidence: []
    gap: Not started; depends on nabhold/baobab-trade integration work.

  - id: ADR-ERP-017
    title: ERP document, attachment, records retention and evidence architecture
    status: planned
    evidence: []
    gap: Not started.

  - id: ADR-ERP-018
    title: ERP reporting, analytics, data export and intelligence integration architecture
    status: planned
    evidence: []
    gap: Not started.

  - id: ADR-ERP-019
    title: ERP provisioning, tenant and legal-entity onboarding, migration and decommissioning architecture
    status: partial
    evidence:
      - modules/provisioning/lifecycle.py
      - tests/unit/test_provisioning_lifecycle.py
      - migration/
    gap: Lifecycle state machine only; no Control Plane wiring and no real migration source system.

  - id: ADR-ERP-020
    title: ERP production readiness, governance and architecture conformance
    status: partial
    evidence:
      - architecture/conformance.yaml
      - docs/migration/00-target-architecture.md
      - docs/migration/01-capability-ownership-matrix.md
    gap: The governance mechanism exists; production readiness itself has not been demonstrated (see the still-planned ADRs above).
