# Extraction

Pulls Party, Product, Warehouse, Account, and transactional records from a live ERPNext
source into a portable, versioned format (e.g. newline-delimited JSON per doctype and
export batch). Empty until Phase 1 (audit) identifies a real source system. See
`../README.md`.
