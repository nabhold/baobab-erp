# Transforms

ERPNext → canonical → iDempiere field-level transformation specifications and the code
that implements them (Party/Customer/Supplier → canonical Party → C_BPartner, Item →
canonical Product → M_Product, and so on — see Phase 11 of the migration programme).
Empty until there is a real source schema to transform from. See `../README.md`.
