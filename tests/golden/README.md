Golden transaction tests (Phase 22) exercise complete business flows end-to-end against a
running iDempiere instance: procurement (PO → receipt → invoice → payment →
reconciliation), sales (commerce order → ERP sales order → shipment → invoice → payment →
allocation → reconciliation), inventory, and at least one multi-currency case. Empty until
`modules/integration` has a real iDempiere client and a test iDempiere instance is
available to run against.
